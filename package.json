{
  "name": "screwzira-downloader",
  "version": "1.0.0",
  "description": "",
  "main": "built/index.js",
  "scripts": {
    "test": "npm run compile && node built/index.js",
    "compile": "tsc",
    "build": "npm run compile && nexe -o dist/screwzira-downloader.exe"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^7.0.0",
    "node-notifier": "^5.2.1",
    "request": "^2.88.0",
    "winston": "^3.0.0"
  },
  "bundledDependencies": [
    "request"
  ],
  "devDependencies": {
    "@types/node": "^10.9.2"
  }
}
